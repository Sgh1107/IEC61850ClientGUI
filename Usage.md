# IEC 61850 客户端 GUI 工具 —— 详细使用手册

> 本手册面向**第一次接触本程序、甚至第一次接触 IEC 61850 的用户**。
> 读完你能：准备好运行环境 → 启动程序 → 连接一台 IED（测试服务器）→
> 浏览数据、读写值、控制开关、订阅报告 → 使用 AI 助手 → 维护点表语义映射。

---

## 1. 这个程序是干什么的？

IEC 61850 是变电站自动化领域的国际标准通信协议。本程序是一个**客户端 GUI 工具**，
可以：

- 连接 IED 设备（间隔层保护装置、测控装置、合并单元等），或本机运行的
  61850 测试服务器（模拟器）
- **浏览**设备的数据模型（LD → LN → DO → DA 层级树）、**读取**实时值、
  **写入**定值/参数
- **遥控**操作（类似开关分合闸，带人工确认）
- **订阅报告**实时接收数据变化事件
- 浏览/上传/下载设备**文件**
- 内置 **AI 助手**：用自然语言对话来操作上述功能
- **点表语义映射**：给晦涩的点名（如 `SPCSO3`）起中文别名并存入本地数据库，
  让人和 AI 都能"看懂"每个点是什么

技术底座：Python + tkinter（ttkbootstrap 美化）+ ctypes 直接调用
**libiec61850** 官方 C 库的动态链接库，不需要编译 C 代码。

---

## 2. 运行前必须准备的东西（重要！）

### 2.1 Python 环境

- 需要 **Python 3.8 及以上**（Windows 自带 Store 版亦可）
- 验证：打开 PowerShell 或 CMD，输入 `python --version` 能看到版本号

### 2.2 安装 Python 依赖（只有一个）

```bash
pip install ttkbootstrap
```

### 2.3 libiec61850 动态库（**必需，缺了程序无法启动**）

程序的所有 61850 通信都通过 `iec61850.dll`（Windows）/ `libiec61850.so`（Linux）
完成。程序会按以下顺序自动查找：

1. 本程序目录（`IEC61850ClientGUI/`）
2. `../build_iec61850_vs2022/src/Debug`
3. `../libiec61850-1.6/MMSFileUI`
4. `../libiec61850-1.6/build_win_vs2022/src/Debug`、`build2/src/Debug`、`build/src`
5. 系统 PATH 搜索路径

**最简单的办法**：把编译好的 `iec61850.dll` 直接复制到本程序根目录
（与 `main.py` 同级）。

- 如果你拿到了完整的项目工程（含 `libiec61850-1.6` 源码目录），按上面第 2~4 条
  路径放置或让脚本自动找到即可
- 如果都没有：到 [libiec61850 的 GitHub](https://github.com/mz-automation/libiec61850)
  下载源码自行编译，或向项目维护者索要对应位数的 dll
- **位数要匹配**：64 位 Python 需要 64 位 dll。dll 损坏或位数不符时程序会自动
  尝试下一个候选路径

### 2.4 一个可以连接的"对端"

没有真实 IED 也没关系，libiec61850 自带**演示服务器**，本手册全程用它演示：

```bash
# 进入 libiec61850 编译产物目录（各版本路径略有差异），运行：
server_example1.exe          # 最简单的演示服务器，默认监听 102 端口
# 或
server_example_reporting.exe # 带报告功能的演示服务器
```

看到 `Listening on port 102` 之类的输出就说明服务器已就绪，**保持这个窗口
不要关**，另开一个窗口运行本程序。

> 端口 102 是 61850 的标准端口。若提示端口被占用/无权限（Linux 上 <1024 的
> 端口需要 root），可给服务器加参数换端口，连接时在 GUI 里填相同端口即可。

---

## 3. 快速上手（5 分钟跑通）

```bash
cd IEC61850ClientGUI        # 进入本目录
python main.py              # 启动（窗口默认最大化）
# 也可以直接带参数：python main.py 127.0.0.1 102
```

1. 顶部标题栏填 **服务器 IP**（本机演示就填 `127.0.0.1`）和**端口**（`102`）
2. 点绿色 **"连 接"** 按钮，右上角变成绿色 `● 已连接` 即成功
3. 切到 **"📂 数据浏览"** 页，逐层双击展开
   `🏠 服务器 → 🏷 LD → 🔷 LN → 📦 DO → 🔹 DA`，点击叶子节点自动读值
4. 想断开就再点一次连接按钮（此时显示红色"断开连接"）

连接成功的瞬间，程序会自动做一批事（都在后台依次完成）：

- 扫描逻辑设备 → 数据集 → RCB → 全部数据对象
- **把全模型快照写入本地数据库 `data/point_map.db`，并自动应用映射规则**
- 状态栏会依次提示，最后一条是
  `点表映射已同步：N 个数据对象入库（IP:端口），规则生成 M 条语义`

---

## 4. 各标签页详细说明

### 4.1 📂 数据浏览

- **模型树（左）**：双击逐层展开，懒加载（点到哪读到哪，不会一次拉全模型）
- **详情面板（右）**：显示选中节点的完整引用和功能约束（FC）下拉框
  - FC 通俗理解：同一个数据的不同"侧面"。`ST`=状态（开关位置）、`MX`=测量
    （电流电压）、`CF`=配置、`SP`=定值、`CO`=控制
- **读取**：选中节点后点"📖 读取"，或勾选"选中即读"自动读
- **当前值**：大字显示，布尔值会变成醒目的橙黄色
- **写入**：在"写入"框输入新值 → 点写入。程序自动识别：
  - `true` / `false` → 布尔
  - `3` / `3.14` → 整数/浮点
  - 其他 → 字符串
  - 快捷按钮提供 `true/false/0/1`
- **读值历史**：底部表格记录最近 100 次读取（时间/引用/值）
- 绿色 **🏷 映射行**：显示该点在点表映射库中的别名/分类/描述（若有）

### 4.2 📋 数据集（DataSet）

数据集 = 一组预先打包好的数据点，服务器按组读/按组报。

1. 下拉框选择数据集引用（连接后已自动扫描填充），点 **📖 读取数据集**
2. 表格列出每个成员的实时值
3. **写值**：选中某行 → 在"新值"框输入 → `✏ 写入选中成员`；
   或输入**逗号分隔**的一串值 `⬇ 写入全部`（数量必须与成员数一致）

### 4.3 🎛 控制操作（遥控）

模拟真实的开关分合闸操作，**必须人工确认**：

1. 下拉选择控制对象（如 `.../GGIO1.SPCSO1`）
2. 选择模式：
   - **直控（direct）**：直接下发
   - **SBO（select-before-operate）**：先"选择"再"操作"，服务器会校验
     选择权，更安全
3. 选择操作值（true=合 / false=分）、操作来源标识、是否启用联锁/同期校验
4. 点操作按钮 → **弹窗显示操作对象和值 → 人工点"是"才真正下发**
5. 底部操作日志记录每次遥控

> ⚠️ 对真实装置操作前，务必确认对象与值；演示服务器随便玩没关系。

### 4.4 📡 报告订阅

让服务器**主动推送**数据变化（而不是轮询去读）：

1. 下拉选择 RCB（报告控制块，连接后已自动扫描 BRCB/URCB）
2. 勾选需要的触发条件（数据变化/品质变化/数据更新/完整性），
   填写触发缓冲时间（BufTm，毫秒）
3. 点 **订阅** → 收到报告会实时出现在事件表格，带"变化原因"列
   （数据变化/总召/完整性等）
4. **总召(GI)**：立刻让服务器把数据集当前全部值推一遍，常用于初次同步
5. 不用了记得 **退订**

### 4.5 🗃 文件服务

浏览 IED 内的文件系统：双击进入目录、选中文件下载到本地、上传本地文件、
删除服务器文件。每个操作都直接操作 IED，删除不可恢复，请谨慎。

> 注意：libiec61850 的 `server_example_files` 服务器要求**在服务器的运行目录**
> 下存在 `vmd-filestore/` 文件夹，否则列目录会报"类型不匹配"（IED_ERROR 22）。

### 4.6 🗺 点表映射（语义映射，重点推荐）

解决"点名看不懂"的问题。详细概念见 README 的
《点表映射与本地持久化》一节，这里讲**手把手操作**：

**界面结构**：工具栏（刷新/应用规则/SCD导入）＋ 左侧点表 ＋ 右侧编辑面板
＋ 底部规则管理。

**第一次使用（连接服务器之后）：**

1. 连接服务器，等状态栏出现"点表映射已同步：N 个数据对象入库"
2. 切到本页，点 `⟳ 刷新点表`，左侧会列出所有点（含规则生成的默认别名）
3. 在搜索框输入 `SPC`、`开关`、`遥测` 等关键词快速定位

**给某个点起人话别名（人工确认流程）：**

1. 左侧点击一个点（如 `simpleIOGenericIO/GGIO1.SPCSO3`）
2. 右侧填写：别名 `1号开关遥控`、分类 `开关量`、描述 `车间1号断路器遥控点`
3. 点 **💾 保存（人工确认）**
4. **弹窗完整列出将要写入的内容**（改过的字段会显示旧值）→ 核对无误点 **是**
5. 完成：来源标记变成"人工"。**人工语义拥有最高优先级**——重新连接、
   重新应用规则、重新导入 SCD 都不会覆盖它

**批量导入 SCD 工程描述（有工程文件时，语义最权威）：**

1. 点 **📄 导入 SCD desc** → 选择 `.scd/.cid/.icd` 文件
2. 弹窗**预览前 8 条映射**（`ref → desc`）和总条数 → 确认导入
3. SCD 里每个 DOI 的 `desc` 会写入语义表（来源=SCD）；
   短描述（≤20字）会同时用作别名

**管理映射规则：**

- 底部规则表列出内置 18 条（`SPCSO→单点控制`、`AnIn→模拟量输入`…）和
  你自建的规则
- 选中规则 → `⏯ 启用/停用`；内置规则**不能删除**（只能停用），自定义规则
  删除前会弹确认
- 新增规则：填 `模式`（如 `MyDev`）→ 选匹配方式（`ref` 引用包含 /
  `do_type` CDC相等 / `ln_class` LN类相等）→ 填别名模板 → `＋ 添加`
- 改完规则点 **⚙ 应用规则** 批量重新生成（人工语义不会被覆盖；
  勾选"覆盖已有语义"可连规则生成的旧语义一起刷新）

**其它实用功能：**

- 搜索框实时过滤；勾选"只看失效点"可查看模型变化后消失的点
- `🗑 清除该点语义`：删掉该点的映射重新来过（会弹确认）
- 右上角统计：`快照 N 点 ｜ 语义 M 条 ｜ 规则 K 条`

**数据库在哪？** `data/point_map.db`，单文件 SQLite，直接复制这个文件就是
完整备份。三张表的结构说明见 README。

### 4.7 🤖 AI 助手（可选功能）

用自然语言操作 IED。首次使用：

1. 切到 AI 助手页，填 **接口地址**（OpenAI 兼容端点，如 DeepSeek
   `https://api.deepseek.com/v1`）、**API Key**（服务商平台申请）
2. 点 `⟳ 获取模型` 拉取模型列表并选择
3. 对话框直接提问，例如：
   - `读一下 SPCSO1 的开关位置`
   - `这个服务器有哪些数据集？`
   - `帮我把 SPCSO1 置位` → **会弹窗要求人工确认，点"是"才执行**

> ⚠️ `ai_settings.json` 以明文保存 API Key，请勿提交到版本库或分享。

---

## 5. 命令行工具（不打开 GUI 也能用）

```bash
# 联机验证：扫描服务器 -> 应用规则 -> 打印映射表，可交互查询
python tests\verify_live.py [IP] [端口]

# SCD 工程描述导入（语义最权威的来源）
python -m core.scd_import 工程.scd --server 127.0.0.1:102 [--ied IED1] [--overwrite]

# 查看数据库总量（快照点数, 语义条数, 规则数）
python -c "from core.model_store import ModelStore; print(ModelStore().stats())"
```

> `--server` 参数必须与 GUI 连接时用的 `IP:端口` 完全一致（如 `127.0.0.1:102`），
> 语义才能和 GUI 浏览到的点对上。

**离线自检（不需要服务器和 dll）：**

```bash
python tests\test_model_store.py   # 数据库与规则引擎，8 个测试
python tests\test_scd_import.py    # SCD 导入器，4 个测试
```

---

## 6. 注意事项与常见问题（FAQ）

### 6.1 安全注意（务必阅读）

| 事项 | 说明 |
| --- | --- |
| 🔴 **遥控写入真实设备** | 对真实 IED 的遥控/写值会**实际改变设备状态**。操作前核对弹窗里的对象名和值；不确定就点"否" |
| 🔴 **API Key 明文保存** | `ai_settings.json` 不加密，勿提交 git、勿分享 |
| 🟡 **写值有防幻觉校验** | AI 助手写入前会校验引用是否真实存在，编造的引用会被拒绝；但这不能替代人工判断 |
| 🟡 **并发限制** | 一个连接同一时刻只能处理一个 MMS 请求，程序已自动串行化，请勿绕过程序直连数据库的同时跑多个程序实例 |
| 🟡 **文件删除不可恢复** | 文件服务页的删除直接作用于 IED 存储 |

### 6.2 常见报错与解决

| 现象 / 报错 | 原因与解决 |
| --- | --- |
| 启动即报 `OSError: 找不到 iec61850.dll` 或加载失败 | 见 2.3：把与 Python 位数一致的 dll 放到程序根目录，或放到任一自动查找路径 |
| 连接超时 / `连接 x.x.x.x:102 失败` | ① 服务器没启动或 IP/端口填错；② 防火墙拦截 102 端口；③ 网络不通（先 `ping` 一下） |
| 连接成功但某页扫描报错 | 单页失败不影响其它页（程序会跳过并继续），查看状态栏红色提示；常见于服务器不支持某服务 |
| 点表映射同步失败 | ① GUI 和 `verify_live.py` 同时在跑，数据库被占用 → 关掉一个；② `data` 目录无写权限 → 换目录重装或以管理员运行 |
| 文件服务报 `IED_ERROR 22 类型不匹配` | `server_example_files` 需要 `vmd-filestore/` 目录（见 4.5） |
| Linux 下绑定 102 端口失败 | 低于 1024 的端口需要 root，或服务器换非特权端口 |
| 读取值显示 `(空)` 或乱码时间 | 部分类型（时间戳/位串）以文本形式显示，属正常 |
| 规则生成的别名不是想要的名字 | 在点表映射页人工覆盖保存即可，人工语义永不覆盖 |
| 连接新服务器后旧的映射不见了？ | 不会丢。每台 IED 按 `IP:端口` 隔离存储；切回原 IP/端口就能看到原来的数据。消失的点只是标记"失效"，语义仍保留 |

### 6.3 日常使用建议

1. **连接顺序**：先启动服务器（或确认 IED 在线）→ 再启动 GUI → 再连接
2. **先只读后写入**：熟悉一个新设备时，先在数据浏览页看，确认理解了每个点
   的含义（可借助点表映射）再尝试写值/遥控
3. **及时备份点表**：人工确认的语义是宝贵的工程积累，定期复制
   `data/point_map.db` 即可
4. **模型变更后**：重新连接会自动刷新快照；用点表映射页的"只看失效点"
   检查哪些点没了、语义是否需要迁移
5. **演示服务器推荐**：`server_example_reporting` 功能最全（模型+数据集+报告+控制），
   适合完整体验所有标签页

### 6.4 目录速查

```
IEC61850ClientGUI/
├── main.py               ← 程序入口（python main.py）
├── iec61850.dll          ← 需要自行放置的动态库（见 2.3）
├── data/point_map.db     ← 点表映射数据库（自动生成，可备份）
├── ai_settings.json      ← AI 配置（使用 AI 助手后生成，含明文 Key）
├── core/                 ← 核心逻辑（无 GUI，可独立复用）
├── gui/                  ← 界面层
├── agent/                ← AI 助手
└── tests/                ← 自检测试 + 联机验证脚本
```

祝你使用顺利！更深层的设计说明（三张表结构、规则优先级、多服务器数据
一致性）请看 `README.md` 的《点表映射与本地持久化》一节。

